name: "HelloDocker"

on:
  pull_request:
  workflow_dispatch:

jobs:
  hello-docker-workflow-job:
    uses: ./.github/workflows/hellodocker.yml
    with:
        who-to-greet: "Mona the Octocat"


      

