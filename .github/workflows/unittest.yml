name: "UnitTest"

on:
  pull_request:
  workflow_dispatch:
  push:

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, world!"
      - name: Execute unit tests
        with:
          args: 'bash ./gradlew testDebug'
        run: ./gradlew testDebug"

